# ════════════════════════════════════════════════════════════════
# sLOt[B] - Environment Configuration
# ════════════════════════════════════════════════════════════════
#
# 📝 Copy this file to .env and customize for your environment
#    cp .env.example .env
#
# ⚠️  IMPORTANT: Change JWT_SECRET in production!
#
# ════════════════════════════════════════════════════════════════

# 🌐 Server Configuration
# Port for Docker host mapping (external access)
SLOTB_HOST_PORT=5000

# 🔐 Security
# JWT secret for token signing - MUST be changed in production!
# Generate a strong secret: openssl rand -hex 32
JWT_SECRET=change-this-to-a-strong-random-secret-in-production

# 🗄️ Database (Optional - defaults provided)
# DATABASE_PATH=/data/bookings.db

# 📤 File Uploads (Optional - defaults provided)
# UPLOAD_DIR=./uploads
# MAX_FILE_SIZE=5242880

# 🌍 Application (Optional - set in config/app-config.json)
# NODE_ENV=production
